* 项目路径: C:\xampp\htdocs\huisen\
* 核心页面: http://localhost:8080/api/quotes.php (报价主页)
* 图片资源池: C:\xampp\htdocs\huisen\images\图片素材\ (里面存放了大量未整理的手机图片)
* 目标参考: 我希望网站的视觉风格和交互体验能达到 九机网 (9ji.com) 或 中关村在线 (ZOL) 的专业水准。
Mission (你的任务): 请帮我对网站进行全方位的重大升级，包括数据治理和前端重构。请按以下步骤执行：
Step 1: 自动化图片匹配脚本 (Python)
我的数据库里有手机型号，文件夹里有图片，但它们没有关联起来。请写一个 Python 脚本 (auto_match_images.py)：
1. 连接数据库: 连接本地 MySQL (默认 XAMPP 配置: host=localhost, user=root, db=甘肃汇森)。
2. 遍历资源: 扫描 C:\xampp\htdocs\huisen\images\图片素材\ 文件夹下的所有图片。
3. 模糊匹配: 读取数据库中的 model (型号) 字段，在文件夹中搜索包含该型号关键词的图片文件,大概每个图片的文件名都对应着手机，每种手机有五个图片，选取最好看的图片作为页面展示图片。
4. 更新数据库: 一旦匹配成功，将图片的相对路径 (例如 images/图片素材/iphone15.jpg) 更新到数据库的中，保证我的网页上能正确显示图片。
5. 输出日志: 打印匹配成功的数量和失败的型号。
Step 2: 首页重构 (quotes.php) - 仿九机网风格
请重写 quotes.php，要求如下：
* 技术栈: 使用 Tailwind CSS (CDN) 进行快速美化，确保无需编译即可运行。
* 配色方案: 采用“九机红”(#e1251b) 作为主色调，搭配干净的白色背景和浅灰边框。
* 布局 (Responsive):
o PC端: 左侧为品牌筛选侧边栏 (Sticky)，右侧为卡片式网格布局 (Grid)，每行显示 5 个手机。
o 移动端: 自动折叠侧边栏为顶部横向滚动条，网格变为双列布局 (每行 2 个)。
* 卡片设计: 必须包含高清大图、加粗型号、醒目的红色价格、以及“维修咨询”按钮。
* 功能区: 顶部增加一个专业的搜索框（带热搜词推荐）和导航条（首页 | 手机回收 | 故障维修 | 手机租赁）。
Step 3: 详情页设计 (detail.php) - 专业级展示
请设计一个全新的详情页：
* 首屏: 左侧是产品大图轮播，右侧是参数选择（颜色/版本）和价格趋势。
* 维修板块 (新增): 在详情页显著位置增加“官方维修报价”板块（如：换屏多少钱、换电池多少钱），提升网站专业性。
* 参数表: 模仿 ZOL 的参数表格，使用斑马纹样式列出详细配置。
Step 4: 数据库结构优化 (SQL)
为了支持上述功能，请提供一段 SQL 语句：
* 增加 repair_price (维修参考价) 字段。
* 增加 tags (标签，如：热销/降价) 字段。
Deliverables (交付要求):
1. 完整的 Python 图片匹配脚本。
2. 重构后的 quotes.php 和 detail.php 完整代码。
3. 必要的 SQL 更新语句。
4. 代码注释要详细，确保我能直接复制到文件里运行。

