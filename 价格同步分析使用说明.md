# 价格同步分析工具使用说明

## 📋 功能说明

这个工具可以帮助您分析为什么很多价格没有匹配上，并提供可视化的报告页面。

## 🚀 使用步骤

### 1. 运行分析脚本

**方法一：通过浏览器访问**
- 在浏览器中访问：`http://localhost/huisen/analyze_price_sync.php`
- 脚本会自动分析 `资料.txt` 文件并生成分析结果

**方法二：通过命令行运行**
```bash
cd C:\xampp\htdocs\huisen
C:\xampp\php\php.exe analyze_price_sync.php
```

### 2. 查看可视化报告

分析完成后，在浏览器中访问：
```
http://localhost/huisen/view_price_analysis.php
```

## 📊 分析报告包含的内容

### 统计卡片
- ✅ **成功匹配**：成功找到并可以更新的记录
- ❌ **数据库未找到**：数据库中不存在匹配的型号
- ⚠️ **关键词不足**：无法提取足够的关键词（需要至少2个）
- 📝 **价格格式错误**：价格格式不符合规则
- 💰 **价格不合理**：价格超出合理范围（100-200000）

### 详细分析标签页

1. **成功匹配**：显示所有成功匹配的记录
   - 原始内容
   - 清理后的型号
   - 提取的关键词
   - 批发价和官网价
   - 匹配到的数据库记录

2. **数据库未找到**：显示无法在数据库中找到的记录
   - 提取的关键词
   - 使用的SQL查询
   - 相似型号建议（如果有）

3. **关键词不足**：显示无法提取足够关键词的记录
   - 已提取的关键词
   - 缺失的关键词类型

4. **价格格式错误**：显示价格格式不符合规则的记录
   - 原始内容
   - 错误原因

5. **价格不合理**：显示价格超出合理范围的记录
   - 计算出的批发价和官网价
   - 错误原因

## 🔍 常见问题分析

### 为什么很多价格没有匹配上？

可能的原因：

1. **关键词提取不准确**
   - 品牌识别失败
   - 型号数字提取失败
   - 容量信息提取失败

2. **数据库中的型号格式不一致**
   - 资料.txt中的格式：`苹果17(3521)-256G黑色(6W4/774)59105999`
   - 数据库中的格式可能不同

3. **匹配逻辑太严格**
   - 需要至少2个关键词才能匹配
   - 所有关键词必须同时匹配

4. **数据库中没有对应的记录**
   - 某些型号在数据库中不存在
   - 需要先添加这些型号到数据库

## 💡 改进建议

1. **优化关键词提取**
   - 扩展品牌识别规则
   - 改进型号数字提取正则表达式
   - 支持更多容量格式

2. **改进匹配逻辑**
   - 使用模糊匹配
   - 降低关键词数量要求
   - 使用相似度算法

3. **数据标准化**
   - 统一资料.txt和数据库中的型号格式
   - 建立型号映射表

## 📁 生成的文件

- `price_sync_analysis.json`：分析结果的JSON数据文件
- `view_price_analysis.php`：可视化报告页面

## 🔄 重新分析

如果需要重新分析，只需再次运行 `analyze_price_sync.php`，它会覆盖之前的分析结果。
