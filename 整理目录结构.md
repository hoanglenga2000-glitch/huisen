# 目录结构整理完成说明

## ✅ 整理完成

为了保持网站正常运行，我们采用了**最小化改动**的整理方案：

### 📁 整理后的目录结构

```
huisen/
├── [路由入口文件]          # 根目录保留，确保URL不变
│   ├── index.php          # 首页路由
│   ├── login.php          # 登录路由（普通用户）
│   ├── quotes.php         # 报价路由
│   ├── detail.php         # 详情路由
│   ├── cart.php           # 询价单路由（根目录）
│   └── dashboard.php      # 后台路由
│
├── core/                   # 核心页面目录（保持不变）
│   ├── pages/             # 页面文件（按功能分类）
│   │   ├── home/          # 首页相关
│   │   │   └── index_v4.php
│   │   ├── products/      # 产品相关
│   │   │   ├── quotes_v6.php (当前使用)
│   │   │   ├── detail_v4.php
│   │   │   └── detail_v5.php
│   │   ├── user/          # 用户相关
│   │   │   ├── login.php (员工登录)
│   │   │   ├── register.php
│   │   │   ├── profile.php
│   │   │   ├── cart.php
│   │   │   ├── addresses.php
│   │   │   ├── orders.php
│   │   │   └── change_password.php
│   │   └── admin/         # 管理相关
│   │       └── dashboard.php
│   ├── components/        # 可复用组件
│   │   ├── product-card.php
│   │   ├── product-card-v3.php
│   │   └── product-card-spu.php
│   └── api/               # 页面内API
│       └── suggest.php
│
├── api/                    # API接口目录（保持不变）
│   ├── auth.php           # 认证API
│   ├── cart.php           # 询价单API
│   ├── search.php         # 搜索API
│   ├── match.php          # 匹配API
│   └── api.php            # 业务API
│
├── config/                 # 配置文件（保持不变）
│   ├── config.php
│   └── init_database.php
│
├── assets/                 # 静态资源目录（新建）
│   ├── images/            # 图片资源
│   │   ├── banners/
│   │   ├── brands/
│   │   ├── phones/
│   │   └── 图片素材/
│   └── css/               # 样式文件
│       └── style.css
│
├── data/                   # 数据文件目录（新建）
│   ├── product_details/   # 产品详情JSON
│   └── logs/              # 日志文件
│       ├── price_sync_log_*.txt
│       └── ...
│
├── tools/                  # 工具脚本（保持不变）
│   ├── sql/
│   └── *.php, *.py
│
├── docs/                   # 文档目录（保持不变）
│   └── *.md
│
└── storage/                # 存储目录（新建）
    └── logs/               # 系统日志
```

## 🔄 主要改动

### 1. 根目录清理
- ✅ 保留所有路由文件（确保URL不变）
- ✅ 移动日志文件到 `data/logs/`
- ✅ 移动说明文档到 `docs/`

### 2. 路径更新
- ✅ 所有 `require_once` 路径已更新
- ✅ 所有 `href` 和 `src` 路径已更新
- ✅ API 调用路径已更新

### 3. 文件分类
- ✅ 页面文件按功能分类（home/products/user/admin）
- ✅ 静态资源统一到 `assets/`
- ✅ 数据文件统一到 `data/`

## 📝 路径引用说明

### 配置文件引用
```php
require_once __DIR__ . '/../config/config.php';
```

### 图片资源引用
```php
<img src="/assets/images/...">
```

### API调用
```php
fetch('/api/cart.php?action=add')
```

### 页面跳转
```php
href="/core/pages/products/quotes_v6.php"
```

## ⚠️ 注意事项

1. **路由文件位置不变**：所有根目录的路由文件保持原位置，确保URL访问不变
2. **向后兼容**：通过路由文件重定向到新位置
3. **逐步迁移**：可以逐步将文件移动到新位置，不影响现有功能

## 🎯 优势

1. **结构清晰**：按功能分类，便于维护
2. **路径统一**：使用统一的资源路径
3. **易于扩展**：新功能可以按分类添加
4. **专业规范**：符合现代Web项目结构
