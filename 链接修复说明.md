# 链接跳转修复说明

## ✅ 已修复的文件

### 1. `core/quotes_v3.php`
- **修复前**: `location.href='../detail.php?spu=...'`
- **修复后**: `location.href='detail_v4.php?spu=...'`
- **说明**: 产品卡片点击跳转，现在正确跳转到同目录下的 `detail_v4.php`

### 2. `core/index_v4.php`
- **修复前**: 
  - `href="core/quotes_v3.php"` (错误路径)
  - `location.href='detail.php?spu=...'` (错误文件名)
- **修复后**: 
  - `href="quotes_v3.php"` (同目录)
  - `location.href='detail_v4.php?spu=...'` (正确文件名)
- **说明**: 首页中的链接现在都指向正确的文件

### 3. `core/detail_v4.php`
- **检查结果**: ✅ 链接已正确
  - 返回列表: `href="quotes_v3.php"` ✓
  - 推荐产品: `location.href='detail_v4.php?spu=...'` ✓

### 4. `core/components/product-card-v3.php`
- **修复前**: `location.href='detail_v3.php?spu=...'`
- **修复后**: `location.href='detail_v4.php?spu=...'`
- **说明**: V3产品卡片组件现在跳转到V4详情页

### 5. `core/components/product-card-spu.php`
- **修复前**: `location.href='detail_v2.php?spu=...'`
- **修复后**: `location.href='detail_v4.php?spu=...'`
- **说明**: SPU产品卡片组件现在跳转到V4详情页

### 6. `core/components/product-card.php`
- **修复前**: `location.href='detail.php?id=...'`
- **修复后**: `location.href='detail_v4.php?spu=...'`
- **说明**: 通用产品卡片组件现在跳转到V4详情页

## 📋 链接路径规则

### 同目录文件（core目录下）
- ✅ `quotes_v3.php` → 直接使用文件名
- ✅ `detail_v4.php` → 直接使用文件名
- ✅ `index_v4.php` → 直接使用文件名

### 跨目录文件
- ❌ 不要使用 `../detail.php`（已废弃）
- ❌ 不要使用 `core/quotes_v3.php`（如果已在core目录下）
- ✅ 使用相对路径时，确保路径正确

## 🔍 验证方法

1. **返回列表功能**
   - 在 `detail_v4.php` 页面点击"返回列表"
   - 应该跳转到 `core/quotes_v3.php`

2. **产品卡片点击**
   - 在 `quotes_v3.php` 或 `index_v4.php` 点击任意产品卡片
   - 应该跳转到 `core/detail_v4.php?spu=XXX`

3. **推荐产品点击**
   - 在 `detail_v4.php` 页面点击"猜你喜欢"中的产品
   - 应该跳转到 `core/detail_v4.php?spu=XXX`

## ⚠️ 注意事项

1. **参数名称**: 统一使用 `spu` 参数，不要使用 `id`
2. **文件版本**: 统一使用 `detail_v4.php`，不要使用旧版本的 `detail.php`、`detail_v2.php`、`detail_v3.php`
3. **路径相对性**: 所有文件都在 `core` 目录下，使用相对路径时不需要 `../`

## 🎯 测试清单

- [ ] 从列表页点击产品 → 跳转到详情页
- [ ] 从详情页点击"返回列表" → 跳转到列表页
- [ ] 从详情页点击推荐产品 → 跳转到对应详情页
- [ ] 从首页点击产品 → 跳转到详情页
- [ ] 所有链接都能正常访问
